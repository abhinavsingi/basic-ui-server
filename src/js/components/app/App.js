import React from 'react';
import Header from '../header';
import Workspace from '../workspace';

const App = () => (
    <div>
        <Header />
        <Workspace />
    </div>
);

export default App;
